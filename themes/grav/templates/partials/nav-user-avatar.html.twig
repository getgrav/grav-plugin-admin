{% set own_avatar_url = admin.user.getAvatarUrl() %}
{% if own_avatar_url %}
    <img src="{{ ('?' not in own_avatar_url) and (not own_avatar_url starts with 'data:') ? own_avatar_url ~ '?s=80' : own_avatar_url }}" alt="Own avatar" />
{% endif %}

