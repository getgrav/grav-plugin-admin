{% extends "forms/field.html.twig" %}

{% block input %}
    <div class="form-display-wrapper {{ field.size }}">
        {% if field.markdown %}
            {{ field.content|markdown }}
        {% else %}
            {{ field.content }}
        {% endif %}
    </div>
{% endblock %}
